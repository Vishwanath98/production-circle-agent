# agent_spine: shared runtime helpers for tenant-safe stateful agents.
#
# events        logic-point event instrumentation (nodes, routers, tools)
# checkpointer  sqlite/memory LangGraph checkpointer selection
# mock_llm      deterministic scripted chat model for offline runs
# rag           framework-free RAG spine (chunking, hashing embedder, retriever)
# mcp_client    sync bridge to an MCP stdio server subprocess
